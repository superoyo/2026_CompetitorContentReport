# Local Claude Code settings — contains API tokens in permission rules. NEVER commit.
.claude/settings.local.json

# Secrets
.env
*.token

# Python
__pycache__/
*.pyc

# Scratch / intermediate scrape output
raw_agencies_chrome.json
slides_output/

# Page profile pictures, downloaded fresh with every scrape and inlined into
# the payload — nothing reads them off disk after the run that made them
page_avatars/
